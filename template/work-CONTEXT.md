# .work/CONTEXT.md — The Shared Workbench
# Generated by mkslf@home v0.5.0

## Ownership Contract

| Room | Owner | Edit rights |
|---|---|---|
| `.work/` (this room) | Shared workbench | **Agent edits REQUIRE operator approval before they stand.** Propose → approve → apply → verify. |
| `.self/` | The agent | Agent edits freely |
| `.operator/` | The operator | Operator-private; agent reads only on knock, never edits |

## What Lives Here
Joint artifacts: builds, deliverables, research, documents produced together.

## Standing Rules
1. Agent proposes changes in chat; operator approves; THEN the file changes.
2. Every directory deeper than this gets its own CONTEXT.md when it earns one.
3. Decisions and learnings go to NOTES.md — this file is the map only.
4. Back up before structural changes (versioned tarball, verified non-zero).